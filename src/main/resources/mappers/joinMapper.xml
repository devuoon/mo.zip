<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mozip.domain.member.JoinRepository">
    <insert id="joinMember">
        INSERT INTO MEMBER(ID, EMAIL, PASSWORD, USERNAME, PHONE)
        VALUES (MEMBER_SEQ.NEXTVAL, #{email}, #{password}, #{username}, #{phone})
    </insert>
</mapper>